[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "misaka-kernel-contracts"
version = "0.1.0"
description = "Generic contracts for the Multi-Agent Composition Kernel"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/misaka_kernel_contracts"]

